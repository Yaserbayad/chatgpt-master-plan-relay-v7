# Q10 result

**Candidate classification:** `INCONCLUSIVE`  
**Rerun safe:** YES — exactly once through UI.Vision with the corrected canonical probe.

Two direct CSV records show that the failed attempts stopped before Project-home activation and before any Send: `send_action_count=0`, `home_click_count=0`, `sidebar_click_count=1`, failure `active Project rows=0`. The current live DOM evidence explains the failure as selector drift and identifies a unique, visible replacement selector based on the active conversation anchor and its ancestor Project group.

The corrected selector and strengthened stable-marker reconciliation have not been executed through UI.Vision. The permitted Codex Chrome surface cannot control the extension page, so no material rerun was performed and no target message is asserted.
